generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}


datasource db {
  provider = "postgresql"
}

model Pessoa {
  id    Int  @id @default(autoincrement())
  nome  String
  cpf   String
  dataNasc  String
}